{
  "name": "ecommerce",
  "version": "1.0.0",
  "description": "mobile app",
  "main": "index.js",
  "engines": {
    "node": "22.x"
  },
  "scripts": {
    "build": "npm install --prefix backend && npm install --prefix admin && npm run build --prefix admin",
    "start": "npm run start --prefix backend"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Breazybsl/ecommerce.git"
  },
  "type": "commonjs",
  "dependencies": {
    "mongodb": "4.1"
  }
}
